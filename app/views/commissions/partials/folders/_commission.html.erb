<div>
  <div class="uk-card uk-card-body uk-card-default uk-card-small">
    <%= link_to commission, class: "uk-link-muted" do %>
      <% if commission.small %>
        <%= image_tag commission.small %>
      <% else %>
        <span class="uk-padding-small" uk-icon="icon: move; ratio: 2"></span>
      <% end %>
    <% end %>
  </div>
</div>
